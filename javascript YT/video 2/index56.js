const value= document.querySelector(".section-todo");
console.log(value);
// note-> we use "classList" so that we can count total no class is assigned                                                                                                                                       
// console.log(value.classList);

// how to add class using js 
value.classList.add('bg-colorValue');